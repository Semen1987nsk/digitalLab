# Цифровая Лаборатория — Строгие правила анализа
# Senior-level: максимальная строгость для предотвращения багов.

include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    missing_return: error
    dead_code: warning
  language:
    strict-casts: true
    strict-raw-types: true

linter:
  rules:
    # Безопасность и корректность
    avoid_print: true
    avoid_relative_lib_imports: true
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_final_fields: true
    prefer_final_locals: true
    always_declare_return_types: true
    annotate_overrides: true
    avoid_empty_else: true
    cancel_subscriptions: true
    close_sinks: true
    
    # Стиль и читаемость
    prefer_single_quotes: true
    # sort_constructors_first: disabled — too many existing violations
    # in data classes / widgets where fields-before-constructor is idiomatic.
    unnecessary_this: true
    use_key_in_widget_constructors: true
    sized_box_for_whitespace: true
    prefer_is_empty: true
    prefer_is_not_empty: true
    
    # Performance
    avoid_unnecessary_containers: true
    prefer_const_constructors_in_immutables: true
