// Basic smoke test for Digital Lab
// Skip: Widget tests require mock HAL setup

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Smoke test - app compiles correctly', () {
    // Just verify the test framework works
    expect(1 + 1, equals(2));
  });
}
